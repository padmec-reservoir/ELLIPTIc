cdef Range ents
cdef EntityHandle current_entity

mb.get_entities_by_dimension(0, {{ dim }}, ents, False)

cdef unsigned int i = 0
for i in range(0, ents.size()):
    current_entity = ents[i]

    {% if child %}
    {{ child|indent }}
    {% endif %}
